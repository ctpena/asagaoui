+++
title = '画像'
summary = 'HTMLのimgに対応した、ページ中に画像を埋め込むためのコンポーネントです。画像キャプションと組み合わせて使うこともできます。'
+++

HTMLの`<img>`に対応した、ページ中に画像を埋め込むためのコンポーネントです。画像キャプションと組み合わせて使うこともできます。

```html {preview="true"}
<figure>
  <img src="/no-image.svg" alt="">
  <figcaption>キャプションテキスト</figcaption>
</figure>
```
